# Parameters for snakemake run

input_file: #Path to input TSV file with list of run accessions in first column and study accessions in 2nd column

output_fastq_dir: #Output directory to store cleaned FASTQs

host_ref: #Path to FASTA file of host genome for decontamination

fasta_dir: #Home directory of the folder with the fasta file of interest

output_align_dir: #Output directory to store alignment files

fasta: #Fasta file name

paladin:
  cores: 6